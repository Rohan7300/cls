<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:animateLayoutChanges="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        android:id="@+id/app_bar_main_activity_two"
        layout="@layout/app_bar_main_activity_two"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:backgroundTint="@color/home_blue"
        android:fitsSystemWindows="true"

        android:theme="@style/NavigationTheme"
        app:drawerLayoutCornerSize="10dp"
        app:headerLayout="@layout/nav_header_main_activity_two"
        app:itemBackground="@color/very_very_light"
        app:itemHorizontalPadding="12dp"
        app:itemIconPadding="12dp"
        app:itemIconSize="18dp"
        app:itemIconTint="@color/black"
        app:itemRippleColor="@color/red"
        app:itemShapeInsetStart="12dp"
        app:itemTextColor="@color/black"
        app:menu="@menu/activity_main_drawer" />
</androidx.drawerlayout.widget.DrawerLayout>